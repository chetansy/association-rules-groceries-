install.packages("arules")
library(arules)

book<-read.csv("C:/Users/blacksnake/Downloads/book.csv")
rules <- apriori(as.matrix(book),parameter=list(support=0.02, confidence = 0.5,minlen=5))


arules::inspect(rules)

rules.sorted<-sort(rules,by="lift")
rules<-apriori(book,parameter = list(minlen=2,supp=0.2,conf=0.5))
arules::inspect(rules)
inspect(head(sort(rules, by = "lift")))  
head(quality(rules))
install.packages("arulesViz")
library(arulesViz)
install.packages("grid")
library(grid)
plot(rules,method = "matrix")
